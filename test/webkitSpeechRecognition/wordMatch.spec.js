var expect = require('chai').expect;
var phrases = require('../assets/webkitResults');
// TODO: export wordMatch
var wordMatch = function (word) {
  return {
    word: word,
    score: 0
  };
};

describe('word match unit test', function () {
  it('should score a word', function (done) {
    done();
  });
  it('should throw out the bad commands', function (done) {
    done();
  });
  it('should match words in test cases', function (done) {
    done();
  });
});
